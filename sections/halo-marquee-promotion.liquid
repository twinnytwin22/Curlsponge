{%- if section.settings.show_marquee -%}
    {%- liquid
        assign padding_top = section.settings.padding_top | append: 'px'
        assign padding_bottom = section.settings.padding_bottom | append: 'px'
        assign padding_top_tablet = section.settings.padding_top_tablet | append: 'px'
        assign padding_bottom_tablet = section.settings.padding_bottom_tablet | append: 'px'
        assign padding_top_mobile = section.settings.padding_top_mobile | append: 'px'
        assign padding_bottom_mobile = section.settings.padding_bottom_mobile | append: 'px'
        assign marquee_speed = section.settings.marquee_speed
        assign padding_top_bottom_item = section.settings.padding_top_bottom_item | append: 'px'
        assign padding_top_bottom_item_mobile = section.settings.padding_top_bottom_item_mobile | append: 'px'
        assign marquee_background_color = section.settings.marquee_background_color
        assign show_border_marquee = section.settings.show_border_marquee
        assign marquee_text_color = section.settings.marquee_text_color
        assign marquee_font = section.settings.marquee_font
        assign marquee_font_size = section.settings.marquee_font_size | append: 'px'
        assign marquee_mobile_font_size = section.settings.marquee_mobile_font_size | append: 'px'
        assign marquee_font_weight = section.settings.marquee_font_weight
        assign marquee_font_style = section.settings.marquee_font_style
        assign marquee_text_transform = section.settings.marquee_text_transform
    -%}
    <div class="halo-block halo-marquee-promotion halo-block-padding" id="halo-marquee-promotion-{{ section.id }}"
        style="--padding-top: {{ padding_top }};
            --padding-bottom: {{ padding_bottom }};
            --padding-top-tablet: {{ padding_top_tablet }};
            --padding-bottom-tablet: {{ padding_bottom_tablet }};
            --padding-top-mobile: {{ padding_top_mobile }};
            --padding-bottom-mobile: {{ padding_bottom_mobile }};"
    >
        <div class="marquee-box{% if show_border_marquee %} marquee-border{% endif %}"
            style="--marquee-speed: {{ marquee_speed | append: 's' }};
            --marquee-speed-mobile: {{ marquee_speed | times: 2 | append: 's' }};
            --padding-top-bottom-item: {{ padding_top_bottom_item }};
            --padding-top-bottom-item-mobile: {{ padding_top_bottom_item_mobile }};
            --marquee-background-color: {{ marquee_background_color }};
            --marquee-text-color: {{ marquee_text_color }};
            {% if marquee_font == 'font_1' %}
                --marquee-font: var(--font-1-family);
            {% elsif marquee_font == 'font_2' %}
                --marquee-font: var(--font-2-family);
            {% elsif marquee_font == 'font_3' %}
                --marquee-font: var(--font-3-family);
            {% endif %}
            --marquee-font-size: {{ marquee_font_size }};
            --marquee-mobile-font-size: {{ marquee_mobile_font_size }};
            --marquee-font-weight: {{ marquee_font_weight }};
            --marquee-font-style: {{ marquee_font_style }};
            --marquee-text-transform: {{ marquee_text_transform }};"
        >
            <div class="banner-marquee">
                {% case section.blocks.size %}
                {% when 1 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign options_marquee = section.blocks[0].settings.options_marquee
                            if options_marquee == 'text'
                                assign text = section.blocks[0].settings.text
                            else
                                assign image = section.blocks[0].settings.image
                            endif
                            assign link = section.blocks[0].settings.link
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 2 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 2
                            if number == 0
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            else
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 3 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 3
                            if number == 0
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            else
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 4 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 4
                            if number == 0
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            else
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 5 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 5
                            if number == 0
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            else
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 6 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 6
                            if number == 0
                                assign options_marquee = section.blocks[5].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[5].settings.text
                                else
                                    assign image = section.blocks[5].settings.image
                                endif
                                assign link = section.blocks[5].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 4
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            else
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 7 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 7
                            if number == 0
                                assign options_marquee = section.blocks[6].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[6].settings.text
                                else
                                    assign image = section.blocks[6].settings.image
                                endif
                                assign link = section.blocks[6].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 4
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            elsif number == 5
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            else
                                assign options_marquee = section.blocks[5].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[5].settings.text
                                else
                                    assign image = section.blocks[5].settings.image
                                endif
                                assign link = section.blocks[5].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 8 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 8
                            if number == 0
                                assign options_marquee = section.blocks[7].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[7].settings.text
                                else
                                    assign image = section.blocks[7].settings.image
                                endif
                                assign link = section.blocks[7].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 4
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            elsif number == 5
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            elsif number == 6
                                assign options_marquee = section.blocks[5].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[5].settings.text
                                else
                                    assign image = section.blocks[5].settings.image
                                endif
                                assign link = section.blocks[5].settings.link
                            else
                                assign options_marquee = section.blocks[6].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[6].settings.text
                                else
                                    assign image = section.blocks[6].settings.image
                                endif
                                assign link = section.blocks[6].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 9 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 9
                            if number == 0
                                assign options_marquee = section.blocks[8].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[8].settings.text
                                else
                                    assign image = section.blocks[8].settings.image
                                endif
                                assign link = section.blocks[8].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 4
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            elsif number == 5
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            elsif number == 6
                                assign options_marquee = section.blocks[5].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[5].settings.text
                                else
                                    assign image = section.blocks[5].settings.image
                                endif
                                assign link = section.blocks[5].settings.link
                            elsif number == 7
                                assign options_marquee = section.blocks[6].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[6].settings.text
                                else
                                    assign image = section.blocks[6].settings.image
                                endif
                                assign link = section.blocks[6].settings.link
                            else
                                assign options_marquee = section.blocks[7].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[7].settings.text
                                else
                                    assign image = section.blocks[7].settings.image
                                endif
                                assign link = section.blocks[7].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 10 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 10
                            if number == 0
                                assign options_marquee = section.blocks[9].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[9].settings.text
                                else
                                    assign image = section.blocks[9].settings.image
                                endif
                                assign link = section.blocks[9].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 4
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            elsif number == 5
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            elsif number == 6
                                assign options_marquee = section.blocks[5].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[5].settings.text
                                else
                                    assign image = section.blocks[5].settings.image
                                endif
                                assign link = section.blocks[5].settings.link
                            elsif number == 7
                                assign options_marquee = section.blocks[6].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[6].settings.text
                                else
                                    assign image = section.blocks[6].settings.image
                                endif
                                assign link = section.blocks[6].settings.link
                            elsif number == 8
                                assign options_marquee = section.blocks[7].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[7].settings.text
                                else
                                    assign image = section.blocks[7].settings.image
                                endif
                                assign link = section.blocks[7].settings.link
                            else
                                assign options_marquee = section.blocks[8].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[8].settings.text
                                else
                                    assign image = section.blocks[8].settings.image
                                endif
                                assign link = section.blocks[8].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% endcase %}
            </div>
            <div class="banner-marquee">
                {% case section.blocks.size %}
                {% when 1 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign options_marquee = section.blocks[0].settings.options_marquee
                            if options_marquee == 'text'
                                assign text = section.blocks[0].settings.text
                            else
                                assign image = section.blocks[0].settings.image
                            endif
                            assign link = section.blocks[0].settings.link
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 2 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 2
                            if number == 0
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            else
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 3 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 3
                            if number == 0
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            else
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 4 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 4
                            if number == 0
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            else
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 5 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 5
                            if number == 0
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            else
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 6 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 6
                            if number == 0
                                assign options_marquee = section.blocks[5].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[5].settings.text
                                else
                                    assign image = section.blocks[5].settings.image
                                endif
                                assign link = section.blocks[5].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 4
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            else
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 7 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 7
                            if number == 0
                                assign options_marquee = section.blocks[6].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[6].settings.text
                                else
                                    assign image = section.blocks[6].settings.image
                                endif
                                assign link = section.blocks[6].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 4
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            elsif number == 5
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            else
                                assign options_marquee = section.blocks[5].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[5].settings.text
                                else
                                    assign image = section.blocks[5].settings.image
                                endif
                                assign link = section.blocks[5].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 8 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 8
                            if number == 0
                                assign options_marquee = section.blocks[7].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[7].settings.text
                                else
                                    assign image = section.blocks[7].settings.image
                                endif
                                assign link = section.blocks[7].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 4
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            elsif number == 5
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            elsif number == 6
                                assign options_marquee = section.blocks[5].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[5].settings.text
                                else
                                    assign image = section.blocks[5].settings.image
                                endif
                                assign link = section.blocks[5].settings.link
                            else
                                assign options_marquee = section.blocks[6].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[6].settings.text
                                else
                                    assign image = section.blocks[6].settings.image
                                endif
                                assign link = section.blocks[6].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 9 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 9
                            if number == 0
                                assign options_marquee = section.blocks[8].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[8].settings.text
                                else
                                    assign image = section.blocks[8].settings.image
                                endif
                                assign link = section.blocks[8].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 4
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            elsif number == 5
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            elsif number == 6
                                assign options_marquee = section.blocks[5].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[5].settings.text
                                else
                                    assign image = section.blocks[5].settings.image
                                endif
                                assign link = section.blocks[5].settings.link
                            elsif number == 7
                                assign options_marquee = section.blocks[6].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[6].settings.text
                                else
                                    assign image = section.blocks[6].settings.image
                                endif
                                assign link = section.blocks[6].settings.link
                            else
                                assign options_marquee = section.blocks[7].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[7].settings.text
                                else
                                    assign image = section.blocks[7].settings.image
                                endif
                                assign link = section.blocks[7].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% when 10 %}
                    {%- for i in (1..10) -%}
                        {%- liquid
                            assign number = forloop.index | modulo : 10
                            if number == 0
                                assign options_marquee = section.blocks[9].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[9].settings.text
                                else
                                    assign image = section.blocks[9].settings.image
                                endif
                                assign link = section.blocks[9].settings.link
                            elsif number == 1
                                assign options_marquee = section.blocks[0].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[0].settings.text
                                else
                                    assign image = section.blocks[0].settings.image
                                endif
                                assign link = section.blocks[0].settings.link
                            elsif number == 2
                                assign options_marquee = section.blocks[1].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[1].settings.text
                                else
                                    assign image = section.blocks[1].settings.image
                                endif
                                assign link = section.blocks[1].settings.link
                            elsif number == 3
                                assign options_marquee = section.blocks[2].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[2].settings.text
                                else
                                    assign image = section.blocks[2].settings.image
                                endif
                                assign link = section.blocks[2].settings.link
                            elsif number == 4
                                assign options_marquee = section.blocks[3].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[3].settings.text
                                else
                                    assign image = section.blocks[3].settings.image
                                endif
                                assign link = section.blocks[3].settings.link
                            elsif number == 5
                                assign options_marquee = section.blocks[4].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[4].settings.text
                                else
                                    assign image = section.blocks[4].settings.image
                                endif
                                assign link = section.blocks[4].settings.link
                            elsif number == 6
                                assign options_marquee = section.blocks[5].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[5].settings.text
                                else
                                    assign image = section.blocks[5].settings.image
                                endif
                                assign link = section.blocks[5].settings.link
                            elsif number == 7
                                assign options_marquee = section.blocks[6].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[6].settings.text
                                else
                                    assign image = section.blocks[6].settings.image
                                endif
                                assign link = section.blocks[6].settings.link
                            elsif number == 8
                                assign options_marquee = section.blocks[7].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[7].settings.text
                                else
                                    assign image = section.blocks[7].settings.image
                                endif
                                assign link = section.blocks[7].settings.link
                            else
                                assign options_marquee = section.blocks[8].settings.options_marquee
                                if options_marquee == 'text'
                                    assign text = section.blocks[8].settings.text
                                else
                                    assign image = section.blocks[8].settings.image
                                endif
                                assign link = section.blocks[8].settings.link
                            endif
                        -%}
                        {%- if options_marquee == 'text' -%}
                            {%- if text != blank -%}
                                <{% if link != blank %}a{% else %}span{% endif %} class="banner-marquee__item text{% if link != blank %} link{% endif %}"{% if link != blank %} href="{{ link }}"{% endif %}>
                                    {{ text }}
                                </{% if link != blank %}a{% else %}span{% endif %}>
                            {%- endif -%}
                        {%- else -%}
                            <div class="banner-marquee__item">
                                {%- if image != blank -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image image-adapt" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        <img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | default: shop.name | escape }}" height="100%" width="100%" loading="lazy">
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- else -%}
                                    <{% if link != blank %}a{% else %}div{% endif %} class="image" {% if link != blank %} href="{{ link }}"{% endif %}>
                                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                                    </{% if link != blank %}a{% else %}div{% endif %}>
                                {%- endif -%}
                            </div>
                        {%- endif -%}
                    {%- endfor -%}
                {% endcase %}
            </div>
        </div>
    </div>
{%- endif -%}
{% schema %}
{
    "name": "t:sections.marquee-promotion.name",
    "settings": [
    {
        "type": "header",
        "content": "t:sections.marquee-promotion.settings.general.name"
    },
    {
        "type": "checkbox",
        "id": "show_marquee",
        "label": "t:sections.marquee-promotion.settings.general.label__1",
        "default": true
    },
    {
        "type": "paragraph",
        "content": "t:sections.general.settings.padding.paragraph__1.content"
    },
    {
        "type": "range",
        "id": "padding_top",
        "label": "t:sections.general.settings.padding.label__1",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "t:sections.general.settings.padding.unit",
        "default": 0
    },
    {
        "type": "range",
        "id": "padding_bottom",
        "label": "t:sections.general.settings.padding.label__2",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "t:sections.general.settings.padding.unit",
        "default": 0
    },
    {
        "type": "paragraph",
        "content": "t:sections.general.settings.padding.paragraph__2.content"
    },
    {
        "type": "range",
        "id": "padding_top_tablet",
        "label": "t:sections.general.settings.padding.label__1",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "t:sections.general.settings.padding.unit",
        "default": 0
    },
    {
        "type": "range",
        "id": "padding_bottom_tablet",
        "label": "t:sections.general.settings.padding.label__2",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "t:sections.general.settings.padding.unit",
        "default": 0
    },
    {
        "type": "paragraph",
        "content": "t:sections.general.settings.padding.paragraph__3.content"
    },
    {
        "type": "range",
        "id": "padding_top_mobile",
        "label": "t:sections.general.settings.padding.label__1",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "t:sections.general.settings.padding.unit",
        "default": 0
    },
    {
        "type": "range",
        "id": "padding_bottom_mobile",
        "label": "t:sections.general.settings.padding.label__2",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "t:sections.general.settings.padding.unit",
        "default": 0
    },
    {
        "type": "header",
        "content": "t:sections.marquee-promotion.blocks.name"
    },
    {
        "type": "range",
        "id": "marquee_speed",
        "label": "t:sections.marquee-promotion.settings.general.options__1",
        "min": 10,
        "max": 30,
        "step": 1,
        "unit": "t:sections.marquee-promotion.settings.general.unit",
        "default": 25
    },
    {
        "type": "range",
        "id": "padding_top_bottom_item",
        "label": "t:sections.marquee-promotion.settings.general.marquee_space",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "t:sections.general.settings.padding.unit",
        "default": 30
    },
    {
        "type": "range",
        "id": "padding_top_bottom_item_mobile",
        "label": "t:sections.marquee-promotion.settings.general.marquee_space_mobile",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "t:sections.general.settings.padding.unit",
        "default": 15
    },
    {
        "type": "header",
        "content": "t:sections.marquee-promotion.settings.general.header__1"
    },
    {
        "type": "color_background",
        "id": "marquee_background_color",
        "label": "t:sections.general.settings.color.label__1",
        "default": "#000000"
    },
    {
        "type": "color",
        "id": "marquee_text_color",
        "label": "t:sections.general.settings.color.label__3",
        "default": "#FFFFFF"
    },
    {
        "type": "checkbox",
        "id": "show_border_marquee",
        "label": "t:sections.marquee-promotion.settings.general.label__2",
        "default": true
    },
    {
        "type": "select",
        "id": "marquee_font",
        "label": "t:settings_schema.typography.settings.font.label",
        "options": [
        {
            "value": "font_1",
            "label": "t:settings_schema.typography.settings.font.options__1"
        },
        {
            "value": "font_2",
            "label": "t:settings_schema.typography.settings.font.options__2"
        },
        {
            "value": "font_3",
            "label": "t:settings_schema.typography.settings.font.options__3"
        }],
        "default": "font_2"
    },
    {
        "type": "range",
        "id": "marquee_font_size",
        "label": "t:settings_schema.typography.settings.font_size.label",
        "min": 15,
        "max": 40,
        "step": 1,
        "unit": "t:settings_schema.typography.settings.font_size.unit",
        "default": 24
    },
    {
        "type": "range",
        "id": "marquee_mobile_font_size",
        "label": "t:settings_schema.typography.settings.font_size.label__2",
        "min": 10,
        "max": 20,
        "step": 1,
        "unit": "t:settings_schema.typography.settings.font_size.unit",
        "default": 18
    },
    {
        "type": "select",
        "id": "marquee_font_weight",
        "label": "t:settings_schema.typography.settings.font_weight.label",
        "default": "700",
        "options": [
        {
            "value": "400",
            "label": "t:settings_schema.typography.settings.font_weight.options__1"
        },
        {
            "value": "500",
            "label": "t:settings_schema.typography.settings.font_weight.options__2"
        },
        {
            "value": "600",
            "label": "t:settings_schema.typography.settings.font_weight.options__3"
        },
        {
            "value": "700",
            "label": "t:settings_schema.typography.settings.font_weight.options__4"
        },
        {
            "value": "800",
            "label": "t:settings_schema.typography.settings.font_weight.options__5"
        },
        {
            "value": "900",
            "label": "t:settings_schema.typography.settings.font_weight.options__6"
        }]
    },
    {
        "type": "select",
        "id": "marquee_font_style",
        "label": "t:settings_schema.typography.settings.font_style.label",
        "options": [
        {
            "value": "normal",
            "label": "t:settings_schema.typography.settings.font_style.options__1"
        },
        {
            "value": "italic",
            "label": "t:settings_schema.typography.settings.font_style.options__2"
        }],
        "default": "italic"
    },
    {
        "type": "select",
        "id": "marquee_text_transform",
        "label": "t:settings_schema.typography.settings.text_transform.label",
        "default": "none",
        "options": [
        {
            "value": "none",
            "label": "t:settings_schema.typography.settings.text_transform.options__1"
        },
        {
            "value": "capitalize",
            "label": "t:settings_schema.typography.settings.text_transform.options__2"
        },
        {
            "value": "uppercase",
            "label": "t:settings_schema.typography.settings.text_transform.options__3"
        },
        {
            "value": "lowercase",
            "label": "t:settings_schema.typography.settings.text_transform.options__4"
        }]
    }],
    "blocks": [
    {
        "type": "marquee",
        "name": "t:sections.marquee-promotion.blocks.name",
        "limit": 10,
        "settings": [
        {
            "type": "select",
            "id": "options_marquee",
            "label": "t:sections.marquee-promotion.settings.general.header__1",
            "options": [
            {
                "value": "text",
                "label": "t:sections.general.settings.content.label__2"
            },
            {
                "value": "image",
                "label": "t:sections.general.settings.image.label__1"
            }],
            "default": "text"
        },
        {
            "type": "image_picker",
            "id": "image",
            "label": "t:sections.general.settings.image.label__1"
        },
        {
            "type": "text",
            "id": "text",
            "label": "t:sections.general.settings.content.label__2",
            "default": "50% Off Everything"
        },
        {
            "type": "url",
            "id": "link",
            "label": "t:sections.general.settings.content.label__4"
        }]
    }],
    "presets": [
    {
        "name": "t:sections.marquee-promotion.presets.name",
        "blocks": [
        {
            "type": "marquee"
        }]
    }]
}
{% endschema%}